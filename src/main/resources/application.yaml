server:
  port: 8080

spring:

  datasource:
    url: jdbc:mysql://mysql:3306/ordersdb
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  application:
    name: order-app

dapr:
  http-port: 3500